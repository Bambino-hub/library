{% extends 'base.html.twig' %}

{% block title %}Edit Book
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="name">Editer un nouveau livre</h1>
		{{ include('book/_form.html.twig', {'button_label': 'Update'}) }}

		<a class="btn btn-warning" href="{{ path('app_book_index') }}">Liste des livres</a>

		{{ include('book/_delete_form.html.twig') }}
	</div>
{% endblock %}
